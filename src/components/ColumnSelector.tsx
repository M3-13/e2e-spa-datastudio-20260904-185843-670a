import type { ReactElement } from 'react';

export function ColumnSelector(): ReactElement {
  return (
    <div className="columnselector">
      <span className="muted">Spaltenauswahl</span>
    </div>
  );
}
