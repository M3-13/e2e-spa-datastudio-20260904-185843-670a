import type { ReactElement } from 'react';

export function Pagination(): ReactElement {
  return (
    <nav className="pagination">
      <span className="muted">Paginierung</span>
    </nav>
  );
}
